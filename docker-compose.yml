version: '3.8'

services:
  credit-risk-api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./models:/app/models   # mount local models folder
      - ./mlruns:/app/mlruns   # if needed for MLflow registry
    environment:
      - MLFLOW_TRACKING_URI=file:/app/mlruns